<% @notifications.each do |notification| %>
  <div class="notification" data-read=<%= notification.read %>>
    <div class='meta'>
      <small><%= time_ago_in_words(notification.created_at) %> ago.</small>
      <%= form_for notification do |f| %>
	<label>Mark Read
	<%= f.check_box :read %>
	</label>
      <% end %>
    </div>
    <%= notification.body %>
  </div>
<% end %>
