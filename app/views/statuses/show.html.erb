<h2><%= @student["name"] %></h2>
<%= render 'form' %>

<% @statuses.each do |status| %>
  <div class='status'>
    <time><%= time_ago_in_words(status.created_at) %> ago</time>
    <p><%= status.body %> </p>
    <div class='color <%= status.color%>'></div>
  </div>
<% end %>
